export interface User {
  name: string;
  role: string;
  photo: string;
  email: string;
  gender: string;
  dob: string;
  _id: string;
}

export interface Product {
  name: string;
  photo: string;
  category: string;
  price: number;
  stock: number;
  _id: string;
}
